/**
 * _abs - compute the absolute value of num n and return absolute value of num
 * @num: number to be checked
 *
 * Return: return the num if num > 0 else num * -1
 */
int _abs(int num)
{
	if (num >= 0)
		return (num);
	else
		return (num * -1);
}
